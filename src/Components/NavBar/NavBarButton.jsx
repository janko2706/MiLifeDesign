import React from 'react';
import { StyledNavBarButton } from './styles';

const NavBarButton = (props) => {
  return <StyledNavBarButton>{props.text}</StyledNavBarButton>;
};

export default NavBarButton;
